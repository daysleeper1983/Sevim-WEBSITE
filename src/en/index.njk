---
layout: base.njk
locale: en
page_key: home
lang_alt_tr: /tr/
lang_alt_en: /en/
title: Home
---
<section class="hero">
  <img class="hero-bg" src="{{ site.hero_photo }}" alt="">
  <div class="hero-inner">
    <p class="eyebrow">{{ site.en.tagline }}</p>
    <h1>{{ site.en.hero_intro }}</h1>
    <div class="hero-actions">
      <a class="btn btn-primary" href="{{ slugs.sessions.en }}">{{ i18n.en.book_now }}</a>
      <a class="btn btn-outline" href="{{ slugs.gallery.en }}">{{ i18n.en.nav_gallery }}</a>
    </div>
  </div>
</section>

<section class="section">
  <div class="container split">
    <div class="split-media">
      <img src="{{ site.about_photo }}" alt="Sevim Elmas">
    </div>
    <div>
      <p class="eyebrow">{{ i18n.en.nav_about }}</p>
      <h2>{{ site.en.about_title }}</h2>
      <p class="lede">{{ site.en.about_body | markdown | truncate(220) | safe }}</p>
      <a class="btn btn-outline" href="{{ slugs.about.en }}">{{ i18n.en.read_more }}</a>
    </div>
  </div>
</section>

<section class="section section-alt">
  <div class="container">
    <div class="section-head">
      <p class="eyebrow">{{ i18n.en.nav_gallery }}</p>
      <h2>{{ site.en.gallery_intro }}</h2>
    </div>
    <div class="grid grid-4">
      {% for item in collections.gallery | featuredOnly | limit(4) %}
      <div class="card">
        <div class="card-media"><img src="{{ item.data.image }}" alt="{{ item.data.en.title }}"></div>
        <div class="card-body">
          <h3>{{ item.data.en.title }}</h3>
          <p>{{ item.data.en.description }}</p>
        </div>
      </div>
      {% endfor %}
    </div>
    <p style="margin-top:36px;"><a class="btn btn-primary" href="{{ slugs.gallery.en }}">{{ i18n.en.view_all }}</a></p>
  </div>
</section>

<section class="section">
  <div class="container split reverse">
    <div>
      <p class="eyebrow">{{ i18n.en.nav_sessions }}</p>
      <h2>{{ i18n.en.nav_sessions }}</h2>
      <p class="lede">{{ site.en.sessions_intro }}</p>
      <a class="btn btn-primary" href="{{ slugs.sessions.en }}">{{ i18n.en.book_now }}</a>
    </div>
    <div class="split-media">
      <img src="/images/placeholders/gallery-mandala.svg" alt="">
    </div>
  </div>
</section>

<section class="section section-alt">
  <div class="container">
    <div class="section-head center">
      <p class="eyebrow">{{ i18n.en.nav_journal }}</p>
      <h2>{{ site.en.journal_intro }}</h2>
    </div>
    <div class="grid grid-3">
      {% for post in collections.journal | sortByDateDesc | limit(3) %}
      <a class="card" href="{{ slugs.journal.en }}{{ post.fileSlug }}/" style="color:inherit;">
        <div class="card-media"><img src="{{ post.data.cover_image }}" alt=""></div>
        <div class="card-body">
          <span class="card-meta">{{ post.data.date | localeDate("en") }}</span>
          <h3>{{ post.data.en.title }}</h3>
          <p>{{ post.data.en.excerpt }}</p>
        </div>
      </a>
      {% else %}
      <p>{{ i18n.en.no_posts_yet }}</p>
      {% endfor %}
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="contact-card">
      <p class="eyebrow" style="color:var(--sand);">{{ i18n.en.nav_contact }}</p>
      <h2 style="color:var(--cream);">{{ site.en.contact_intro }}</h2>
      <a class="btn btn-primary" href="{{ slugs.contact.en }}">{{ i18n.en.send_message }}</a>
    </div>
  </div>
</section>
